#include<stdio.h>
Int main(){
Int j=0;
Int i,len,k,a[]={4,5,6,3,56,7,9};
Printf(“请输入一个目标数字：”);
Scanf(“%d”,&k);
Len=sizeof(a)/sizeof(a[0]);
for (i=0;i<len;i++)
{
If (a[i]==k){
printf(“%d”,i);
j=1;
}
}
If(j==0)
printf(“-1”);
}
